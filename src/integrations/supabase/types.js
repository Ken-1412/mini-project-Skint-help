// Database type definitions converted to plain JavaScript
// These are kept as documentation but not enforced at runtime

export const Database = {
    public: {
        Tables: {
            profiles: {
                // Row structure: id, email, name, role, status, etc.
                // role: 'admin' | 'restaurant' | 'worker' | 'receiver'
                // status: 'active' | 'pending' | 'blocked'
            }
        },
        Views: {},
        Functions: {},
        Enums: {},
        CompositeTypes: {}
    }
};

export const Constants = {
    public: {
        Enums: {},
    },
};
